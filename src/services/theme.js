import {createTheme} from "@mui/material";

export let theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: '#0052cc',
    },
    secondary: {
      main: '#edf2ff',
    },
  },
});
