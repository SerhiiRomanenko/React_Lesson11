import {createTheme} from "@mui/material";

export let theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: '#00BA76',
    },
    secondary: {
      main: '#edf2ff',
    },
  },
});
